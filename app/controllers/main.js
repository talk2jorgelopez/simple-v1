/**
 * Created by contactjorge on 11/4/16.
 */
var myPortfolioCtrl = angular.module ('myPortfolioCtrl', []);

myPortfolioCtrl.controller('MainCtrl', ['$scope', '$http', '$routeParams', function ($scope, $http, $routeParams)
{
	
}]);

myPortfolioCtrl.controller('AboutCtrl', ['$scope', '$http', '$routeParams', function ($scope, $http, $routeParams)
{
	/*
	$http.get("js/data.json").success(function(data) {
		console.log("success " + data);
		$scope.authors = data;
	});
	*/
}]);
